# Backend

This folder is intended to hold backend/service code (Node/Express, FastAPI, etc.), API routes, and server configuration.

Put server source under `backend/src` and add a README with run/setup instructions here.
