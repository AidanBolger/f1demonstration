# Frontend

This folder is intended to hold frontend applications (React/Vite, Next.js, etc.).

Currently the `react-app` scaffold lives at `f1demonstration\react-app` — you can move it into this folder as `frontend/react-app` for a cleaner layout.
